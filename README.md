# misskaur
replace with your api keys

pip install openai
pip install pygame
pip install SpeechRecognition   
pip install gtts     
pip install os  
pip install PyAudio
python kaur.py // for final run in terminal
